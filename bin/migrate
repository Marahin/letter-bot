atlas migrate apply \
    --url "postgres://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?search_path=public&sslmode=${DATABASE_SSL}" \
    --revisions-schema atlas_schema_revisions \
    --dir "file://internal/infrastructure/db/postgresql/migrations"
